{
  "name": "@family-messenger/crypto",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "//": "Указываем на TS-исходники, а не на dist/: dist в .gitignore, поэтому на сборщике EAS его нет — Metro компилирует TS сам, отдельный build-шаг для клиента не нужен.",
  "main": "src/index.ts",
  "react-native": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/libsodium-wrappers": "^0.7.14",
    "@types/node": "^22.10.5",
    "libsodium-wrappers": "^0.7.15",
    "react-native-libsodium": "^1.7.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
