-- Когда участник последний раз был на связи. Нужно, чтобы показать «был(а) в
-- 14:32» вместо пустоты, когда человека нет в сети. Сам факт «в сети сейчас»
-- в базе не хранится — он берётся из реестра открытых соединений.
ALTER TABLE users ADD COLUMN last_seen_at INTEGER;
